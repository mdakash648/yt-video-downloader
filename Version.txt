# UTF-8
#
# PyInstaller version-info resource file.
# Used with: pyinstaller --version-file version.txt ...
# This embeds proper file/version metadata into the exe (visible in
# Windows Explorer -> right-click exe -> Properties -> Details tab).
# A "blank" exe with no publisher/product/version info tends to look more
# suspicious to Windows SmartScreen and some antivirus heuristics than one
# with normal-looking metadata filled in. Edit the values below as you like.

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers / prodvers: (major, minor, patch, build) - keep 4 numbers
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    # Contains a bitmask that specifies the valid bits 'flags'
    mask=0x3f,
    # Contains a bitmask that specifies the Boolean attributes of the file
    flags=0x0,
    # The operating system for which this file was designed (0x4 = NT)
    OS=0x40004,
    # The general type of file
    fileType=0x1,
    # The function of the file
    subtype=0x0,
    # Creation date and time stamp
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'040904B0',
        [StringStruct(u'CompanyName', u'Akash'),
        StringStruct(u'FileDescription', u'YT-DLP Downloader - YouTube video/audio/playlist downloader GUI'),
        StringStruct(u'FileVersion', u'1.0.0.0'),
        StringStruct(u'InternalName', u'YTDLPDownloader'),
        StringStruct(u'LegalCopyright', u'Copyright \u00A9 2026 Akash'),
        StringStruct(u'OriginalFilename', u'YTDLPDownloader.exe'),
        StringStruct(u'ProductName', u'YT-DLP Downloader'),
        StringStruct(u'ProductVersion', u'1.0.0.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
